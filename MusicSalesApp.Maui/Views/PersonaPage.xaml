<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:vm="clr-namespace:MusicSalesApp.Maui.ViewModels"
             x:Class="MusicSalesApp.Maui.Views.PersonaPage"
             x:DataType="vm:PersonaViewModel"
             Title="{Binding PersonaName}"
             BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}">

    <ScrollView>
        <VerticalStackLayout Spacing="16"
                             Padding="20,24,20,16">

            <!-- Persona Name -->
            <Label Text="{Binding PersonaName}"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light=Black, Dark=White}"
                   HorizontalTextAlignment="Center" />

                     <!-- Persona Image (actual) -->
                     <Grid HorizontalOptions="Center"
                              HeightRequest="120"
                              WidthRequest="120"
                              IsVisible="{Binding HasImage}">
                            <Image Source="{Binding PersonaImageSource}"
                                      Aspect="AspectFill"
                                      HeightRequest="120"
                                      WidthRequest="120">
                                   <Image.Clip>
                                          <EllipseGeometry Center="60,60"
                                                                       RadiusX="60"
                                                                       RadiusY="60" />
                                   </Image.Clip>
                            </Image>
                     </Grid>

            <!-- Placeholder Image (when no persona image) -->
            <Border HorizontalOptions="Center"
                    StrokeShape="RoundRectangle 60"
                    StrokeThickness="0"
                    HeightRequest="120"
                    WidthRequest="120"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                    IsVisible="{Binding HasImage, Converter={toolkit:InvertedBoolConverter}}">
                <Label Text="&#x266B;"
                       FontSize="48"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray300}}" />
            </Border>

            <!-- Bio -->
            <Label Text="{Binding PersonaBio}"
                   FontSize="15"
                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                   LineBreakMode="WordWrap"
                   Margin="8,0"
                   IsVisible="{Binding HasBio}" />

            <!-- No bio available -->
            <Label Text="No bio available."
                   FontSize="15"
                   FontAttributes="Italic"
                   TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                   HorizontalTextAlignment="Center"
                   Margin="8,0"
                   IsVisible="{Binding HasBio, Converter={toolkit:InvertedBoolConverter}}" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
