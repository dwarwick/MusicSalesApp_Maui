<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MusicSalesApp.Maui.Views.FacebookShareButton">

    <Border x:Name="FbShareBorder"
            StrokeShape="RoundRectangle 4"
            Stroke="Transparent"
            BackgroundColor="{StaticResource FacebookBrand}"
            Padding="0"
            HeightRequest="{StaticResource ControlHeightSmall}"
            WidthRequest="{StaticResource ControlHeightSmall}"
            VerticalOptions="Center">
        <Border.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnFbShareClicked" />
        </Border.GestureRecognizers>
        <Grid Padding="5,0"
              VerticalOptions="Center">
            <!-- Facebook "f" logo area -->
            <Border StrokeShape="RoundRectangle 3"
                    Stroke="Transparent"
                    BackgroundColor="{StaticResource FacebookBrandPressed}"
                    HeightRequest="22"
                    WidthRequest="22"
                    Padding="0"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                <Label Text="f"
                       FontSize="{StaticResource FontSizeBodyLarge}"
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
            </Border>
        </Grid>
    </Border>
</ContentView>
