<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MusicSalesApp.Maui.Views.FacebookShareButton">

    <Border x:Name="FbShareBorder"
            StrokeShape="RoundRectangle 4"
            Stroke="Transparent"
            BackgroundColor="#3b5998"
            Padding="0"
            HeightRequest="32"
            VerticalOptions="Center">
        <Border.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnFbShareClicked" />
        </Border.GestureRecognizers>
        <HorizontalStackLayout Spacing="4" Padding="8,0" VerticalOptions="Center">
            <!-- Facebook "f" logo area -->
            <Border StrokeShape="RoundRectangle 3"
                    Stroke="Transparent"
                    BackgroundColor="#334b82"
                    HeightRequest="22"
                    WidthRequest="22"
                    Padding="0"
                    VerticalOptions="Center">
                <Label Text="f"
                       FontSize="16"
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
            </Border>
            <Label Text="Share"
                   FontSize="14"
                   FontAttributes="Bold"
                   TextColor="White"
                   VerticalOptions="Center" />
        </HorizontalStackLayout>
    </Border>
</ContentView>
